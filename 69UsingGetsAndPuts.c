#include<stdio.h>

int main()
{
    char st[20];
    printf("Enter a line: ");
    gets(st);// it takes any number of strings seperated with spaces
    puts(st);// it shows given input as output
    
    return 0;
}