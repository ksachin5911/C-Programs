#include<stdio.h>
#include<string.h>

int main()
{
    char st[] = "indian";
    int length = strlen(st);//used to calculate length of a string
    printf("The length of given string is %d", length);
    return 0;
}